# Heat_Sim
Compiler Command:
gfortran module_random.f90 main.f90 -lopenblas -O3 -o main.exe; ./main.exe

Generates a random set of heat values and simulates the movement of that heat through a material.
